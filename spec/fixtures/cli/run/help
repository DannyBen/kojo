Run and process a config file

Usage:
  kojo run CONFIG [--save DIR] [ARGS...]
  kojo run (-h|--help)

Options:
  -s --save DIR
    Save output to directory instead of printing

  -h --help
    Show this help

Parameters:
  ARGS
    Optional key=value pairs

Examples:
  kojo run config.yml
  kojo run config.yml --save output
  kojo run config.yml -s output scale=3
